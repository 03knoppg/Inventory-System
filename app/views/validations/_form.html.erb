<%= form_for(@validation) do |f| %>
  <% if @validation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@validation.errors.count, "error") %> prohibited this validation from being saved:</h2>

      <ul>
      <% @validation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<table>
      <tr>
        <th align="left">Extension</th>
        <th align="left">Extension Type</th>
        <th align="left">Actions</th>
      </tr>
      <tr>
        <td style="width: 200px">
          <div class ="field">
            <%= f.text_field :extension, :required => true%>
          </div>
        </td>
        <td>
          <div class="field">
             <%= select_tag "kind", raw("<option>Image</option><option>Data File</option>".html_safe) %>
          </div>
        </td>
        <td>
          <div class="actions">
            <%= f.submit %>
          </div>
        </td>
    </tr>
</table>
<% end %>
<br class="clear" />
