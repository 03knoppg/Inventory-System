<%= form_for(@validation) do |f| %>
  <% if @validation.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@validation.errors.count, "error") %> prohibited this <%= @validation_alias %> from being saved:</h2>

      <ul>
      <% @validation.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
 <div class="main_div">
    <table class="main_table">
      <tr>
        <th align="left">Extension&nbsp;<span style="color: red">*</span></th>
        <th align="left">Extension Type&nbsp;<span style="color: red">*</span></th>
      </tr>
      <tr>
        <td>
          <div class ="field">
            <%= f.text_field :extension, :required => true, :size=>20%>
          </div>
        </td>
        <td>
             &nbsp;<span style="color: red; vertical-align: top;">*</span>
          <div class="field">
            <%= radio_button_tag "kind", "Data File"%> Data File  <br/>
            <%= radio_button_tag "kind", "Image"%> Image

          </div>
        </td>
    </tr>
</table>
   </div>

          <div class="right_div">
        <input type="reset" />
            <%= f.submit :class => "submit"%>
          </div>
<% end %>
<h5>Required fields are marked with <span style="color: red">*</span></h5>
<br class="clear" />
