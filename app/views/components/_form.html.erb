<%= form_for(@component) do |f| %>
  <% if @component.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@component.errors.count, "error") %> prohibited this component from being saved:</h2>

      <ul>
      <% @component.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

             Parent

  <div class ="field">
       <select size="5" multiple="multiple" name="new_components_ids[]">
        <% for comp in @all_components  %>
            <option value="<%= comp.id %>"
                    <% if (@associated_components.include?(comp)) %> selected=true
            <% end %>> <%= comp.name %></option>

        <% end %>
      </select>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %><br />
    <%= f.label :description %><br/>
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
