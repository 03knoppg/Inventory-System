<%= form_for(@component) do |f| %>
  <% if @component.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@component.errors.count, "error") %> prohibited this component from being saved:</h2>

      <ul>
      <% @component.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

             Parent

  <div class ="field">
    <select name="component[parent_id]">

      <% @all_components.each do |component| %>
      <option value= "<%=component.id %>"<% if(@component.parent_id == component.id) %>
            selected = true;
          <% end %>>
        <%= component.name%></option>
      <% end %>
    </select>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
