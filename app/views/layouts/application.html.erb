<!DOCTYPE html>
<html>
<head>
  <title>TestDBRubymineProject</title>
  <%= stylesheet_link_tag 'application', :media => 'screen'%>
  <script type="text/javascript" src="/assets/sortable_us.js?body=1"></script>
  <script type="text/javascript" src="/assets/table.js?body=1"></script>
  <%= javascript_include_tag "application"%>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="container">
  <div class="containerBanner">
    <% banner %>
    <%=image_tag(@imagelogo,  :alt=>"Company Logo", :style => 'margin-right: 5px') %><%=image_tag(@imagebanner,  :alt=>"Company Banner", :width => 945, :height => 100) %>
  </div>
  <div class="containerLinks">
    <nav class="round">
      <ul>
        <%= link_to "Main Menu", "/admin"%> |
        <% if current_user %>
            Logged in as <%= current_user.username %>.
            <%= link_to "Log out", logout_path %>
        <% else %>
            <%= link_to "Sign up", signup_path %> |
            <%= link_to "Log in", login_path %>.
        <% end %>
      </ul>
    </nav>
  </div>
  <br /><br />
  <div class="error_div">
    <% flash.each do |name, msg| %>
        <h4><%= content_tag :div, msg, :id => "flash_#{name}", :class => "error_div"%></h4>
    <% end %>
  </div>
  <br />
  <%= yield %>
</div>
</body>
</html>